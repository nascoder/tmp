name: PR
on: 
  push:
    branches: update

jobs:
  test:
    name: PR
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: pr
        run: |
          echo "hi"
          
